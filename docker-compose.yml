version: "3.8"

services:
  backend:
    build: ./backend
    container_name: shop-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: always

  frontend:
    build: ./frontend
    container_name: shop-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
